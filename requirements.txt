# Deliberately loose upper bounds: a wrong hard pin breaks "clone and run in 5 minutes",
# which costs more than a dependency drift ever would.
streamlit>=1.49,<2.0
pandas>=2.2,<3.0
plotly>=5.22,<7.0
google-genai>=0.3.0
python-dotenv>=1.0,<2.0
reportlab>=4.0,<5.0
Pillow>=10.0,<12.99  # downscales oversized image scans before the extraction call
