# Secrets — never commit. `.env.example` is the only env file that ships.
.env
.env.*
!.env.example
*.key
*.pem
.streamlit/secrets.toml

__pycache__/
*.pyc
.venv/
venv/

# Local seeded database — rebuilds itself from data/seed.py on first run.
data/*.db

# Internal strategy / planning notes — not part of the deliverable.
Assignment.md
PR_Handover_OnePager.md

# Part 2 runtime state — the simulated mailbox and CG outbox fill at demo time.
su_inbox/
cg_outbox/

# Internal planning / PM docs — not part of the deliverable.
approach_part2.md
PM_Decision_Log.docx

# Word owner/lock files, created while a .docx is open.
~$*
